# Optional: install for textbook RAG (upload PDF per module, AI tutor queries it).
# pip install -r requirements-rag.txt
# Now uses Pinecone (hosted) instead of ChromaDB to avoid memory issues on Railway.
# Package was renamed from pinecone-client to pinecone.
pinecone>=5.0.0
